// Q20
const phone = {
  brand : "Apple",
  price : 990000,
  model : "iPhone 13",
};

const { brand, price } = phone;

console.log("브랜드: " + brand); 
console.log("가격: " + price); 

// Q21
const car = {
  brand1 : "Tesla",
  model : "model S",
  color : "red",
};

const { brand1, model} = car;
console.log("차 브랜드: " + brand1);
console.log("모델명: " + model);